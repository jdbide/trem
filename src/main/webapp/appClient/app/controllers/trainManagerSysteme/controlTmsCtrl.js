'use strict';

/**
 * Contrôleur qui gère la page "Control" du chapitre "Train manager systeme", 
 *
 */
socle_app.controller("controlTmsCtrl", ["$rootScope", "$scope", "envService", '$interval',"importTmsService",
                                 function($rootScope, $scope, envService, $interval, importTmsService) {
	$scope.title = "Control";
	
	
}]);